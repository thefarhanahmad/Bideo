In my project all UI part is ready, and its working perfect with dummy data also, now i have to inhance more things in my project to make this more better by adding more features and things.

- Add User model a channel name section, which will be users channel name and about field. so that user can create their channel name.
- For everywhere : everywhere text is "Subscribers" should be "Followers" , "subscribe" to "Follow" , means i want to change subscribe things to follow things properly, so handle this everywhere properly.
- Homescreen : with video , right now showing users name, it should be show channel name, fallback to name.
- Shorts screen : same show channel name || user name, when user will click on any buttons, like follow,like,comment if user not login navigate to login, if already loggedin it should work everything properly
- Upload screen: when i click on Upload screen , if user authenticated already then show upload screen to upload, otherwise login screen.
- On Homescreen : Header will have a search button, after click on that user can search any videos, it should work properly. handle this properly to match videos title || description || tags . and then show the results properly.
- Upload screen : there will be fields when upload - video,thumbnail,title,description,tags,publish select drpwodn (public,private).
- All should be reflect instant, like if we did like any video,comment,follow/unfollow should be instant reflact like count increase/decrease, toggle buttons handling should be properly. everything should be happened aligned with backend. and when we login/logiut again these should be persist like if we had liked,follow/unfollow should be working properly.
- videoDetails screen : when i click on any long video there is buttons for like/dislike, share and download,comment,subscribe.
  download button should be remove from here, i dont want to keep feature for download. only like,dislike and share. and instead of download button keep "Add" which will be added to users playlist.
- same here channel name || name , follow/unfollow and followers , instaed of subcsriber things.
- all buttons should work properly, and should be properly aligned with backend.
- and all these buttons required to authenticated user , so take care of these.
- Subscription Screen : change this screen from "Subscription" to "Followings" , same here all channels with channel name || name and all vidoes from followed channels, make this UI functional working, otherwise fallback UIs are fine.
- Library Screen Tab in bottom : if user click on Library and if user is not loggedin show login screen otherwise open library screen.
- Library screen, on top show User Profile name,number/email and avatar, channel name and about. and there will be a button to create channel.
- create channel will be a screen where user create channel by fill channelname and about, also avatar can change there.
- If channel alrady created edit button, for edit channel for same fields.
- then show History which is already shwing, just make this perfect, where actual users watched videos history will show, make this working , may be need to create backend things. view all will be open "History screen" new created screen, where all history can be show.
- Your Videos : this will be a screen, where users own uploaded videos will be show, can be manage like crud oeprations here. Your videos will be same UI like history, like show two videos and button for View All.
- Playlist : this should be same as above UIs, where all added videos into playlists will be visible. now new playlist, just user wil add to playlist, and will be show here for that users added.simple make this funcional an prolery.
- On top header corner, there will be a button for Logout, where user can logout the profile. should be working properly

all UI designs and functionalities should be perfect, and system shuld be production level scalable system. dont break anything working system , everything should be work perfectly, UIs should be responsive.
